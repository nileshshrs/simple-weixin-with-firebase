// contact_page.dart
import 'package:flutter/material.dart';

class ContactPage extends StatelessWidget {
  static const String routeName = '/contact';

  const ContactPage();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('Contact Page Content'),
    );
  }
}