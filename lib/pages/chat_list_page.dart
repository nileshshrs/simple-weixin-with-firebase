import 'package:flutter/material.dart';

class ChatListPage extends StatelessWidget {
  static const String routeName = '/chatList';

  const ChatListPage();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('Chat List Page Content'),
    );
  }
}